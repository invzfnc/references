example of cache across systems

1. cpu cache - between cpu and ram, uses temporal + spatial locality
2. disk cache (ram buffer) - holds data before writing to disk
3. file system cache (os level) - keeps recently opened file in ram, so reopening is faster
4. browser cache - stores images, scripts, pages, so websites load faster next time
5. application cache - example: store api results temporarily
