Creating element (puts in memory)
```js
document.createElement(tagName, [options]);
```

Adding into DOM
```js
parentNode.appendChild(childNode);
parentNode.insertBefore(newNode, referenceNode);
```

Removing elements
```js
parentNode.removeChild(childNode);
```