segments are sections of a program that have different purposes.

- `.data`: define variables
- `.code`: defines area of program containing executable instructions
- `.stack 100h`: area of program holding the runtime stack, setting its size